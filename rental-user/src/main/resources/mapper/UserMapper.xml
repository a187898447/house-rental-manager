<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rental.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.rental.user.entity.User">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="unionid" property="unionid"/>
        <result column="phone" property="phone"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE openid = #{openid} LIMIT 1
    </select>

    <select id="selectByUnionid" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE unionid = #{unionid} LIMIT 1
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE phone = #{phone} LIMIT 1
    </select>
</mapper>
